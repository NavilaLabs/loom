[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
application = { path = "../../../../core/crates/application" }

dioxus = { workspace = true, features = ["fullstack"] }

[features]
server = ["dioxus/server"]
