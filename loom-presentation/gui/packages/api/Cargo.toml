[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = ["fullstack"] }
loom = { path = "../../../../loom", optional = true }
# tokio-util = { version = "0.7", optional = true }

[features]
# default = ["server"]
server = ["dioxus/server", "dep:loom", "sqlite"]
postgres = ["loom/postgres"]
sqlite = ["loom/sqlite"]
