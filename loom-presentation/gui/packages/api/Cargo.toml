[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = ["fullstack"] }
loom-core = { path = "../../../../loom-core", optional = true }

[features]
server = ["dioxus/server", "loom-core"]
